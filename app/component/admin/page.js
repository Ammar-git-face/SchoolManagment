"use client"
import Sidebar from "./sidevar";
import Hero from "../admin/hero";
const home = () => {
    return (
        <div>

            
            < Hero className=""/>
            <Sidebar />
        </div>
    );
}

export default home